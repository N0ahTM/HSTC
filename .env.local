# Local dev overrides for Vite-only environment variables
# If you want to point the frontend to a remote URL for the API, set VITE_DISCORD_IMAGES_ENDPOINT.
# By default we use the dev middleware at /api/discord-images.
# VITE_DISCORD_IMAGES_ENDPOINT=http://localhost:5173/api/discord-images
